<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:sfTreeView="clr-namespace:Syncfusion.XForms.TreeView;assembly=Syncfusion.SfTreeView.XForms"
             xmlns:behaviors="clr-namespace:DiagnosticNP.Behaviors"
             x:Class="DiagnosticNP.Views.MainPage"
             Title="Вибродиагностика">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Панель статуса -->
        <Frame Grid.Row="0" BackgroundColor="LightBlue" Padding="10" Margin="5">
            <Label Text="{Binding StatusMessage}" 
                   FontSize="14" 
                   TextColor="Black"
                   HorizontalTextAlignment="Center"/>
        </Frame>

        <!-- Панель поиска -->
        <StackLayout Grid.Row="1" Orientation="Horizontal" Padding="10" Spacing="10">
            <Entry Placeholder="Поиск по оборудованию..." 
                   Text="{Binding SearchText}"
                   HorizontalOptions="FillAndExpand"/>
            <Button Text="🗘" 
                    Command="{Binding LoadEquipmentCommand}"
                    BackgroundColor="LightGray"
                    WidthRequest="40"/>
        </StackLayout>

        <!-- Панель управления -->
        <StackLayout Grid.Row="2" Orientation="Horizontal" Padding="10" Spacing="5">
            <Button Text="Загрузить структуру" 
                    Command="{Binding LoadEquipmentCommand}"
                    BackgroundColor="Green"
                    TextColor="White"
                    HorizontalOptions="FillAndExpand"/>

            <Button Text="Выгрузить замеры" 
                    Command="{Binding UploadMeasurementsCommand}"
                    BackgroundColor="Blue"
                    TextColor="White"
                    HorizontalOptions="FillAndExpand"/>

            <Button Text="Очистить" 
                    Command="{Binding ClearMeasurementsCommand}"
                    BackgroundColor="Red"
                    TextColor="White"
                    HorizontalOptions="FillAndExpand"/>
        </StackLayout>

        <!-- Дерево оборудования с Behavior -->
        <sfTreeView:SfTreeView Grid.Row="3"
                               x:Name="treeView"
                               ItemsSource="{Binding EquipmentNodes}"
                               NotificationSubscriptionMode="CollectionChange"
                               ChildPropertyName="Children"
                               AutoExpandMode="None"
                               SelectionMode="Single"
                               SelectedItem="{Binding SelectedNode}"
                               Indentation="20"
                               ExpanderWidth="40">

            <sfTreeView:SfTreeView.Behaviors>
                <behaviors:TreeViewBehavior 
                    ExpandAll="{Binding ExpandAll, Mode=TwoWay}"
                    CollapseAll="{Binding CollapseAll, Mode=TwoWay}"
                    ExpandToNode="{Binding NodeToExpand, Mode=TwoWay}"/>
            </sfTreeView:SfTreeView.Behaviors>

            <sfTreeView:SfTreeView.ItemTemplate>
                <DataTemplate>
                    <Grid Padding="10,5">
                        <Label Text="{Binding Name}" 
                               VerticalOptions="Center"
                               FontSize="14"/>
                    </Grid>
                </DataTemplate>
            </sfTreeView:SfTreeView.ItemTemplate>

        </sfTreeView:SfTreeView>

        <!-- Панель действий -->
        <StackLayout Grid.Row="4" Orientation="Horizontal" Padding="10" Spacing="10">
            <Button Text="📈 Сделать замер" 
                    Command="{Binding TakeMeasurementCommand}"
                    BackgroundColor="DarkGreen"
                    TextColor="White"
                    HorizontalOptions="FillAndExpand"
                    HeightRequest="50"/>

            <StackLayout Orientation="Vertical" Spacing="5">
                <Button Text="📂 Развернуть" 
                        Command="{Binding ExpandAllCommand}"
                        BackgroundColor="Gray"
                        TextColor="White"/>
                <Button Text="📁 Свернуть" 
                        Command="{Binding CollapseAllCommand}"
                        BackgroundColor="Gray"
                        TextColor="White"/>
            </StackLayout>
        </StackLayout>

        <!-- Индикатор загрузки -->
        <ActivityIndicator Grid.Row="3" 
                          Grid.RowSpan="2"
                          IsVisible="{Binding IsLoading}"
                          IsRunning="{Binding IsLoading}"
                          VerticalOptions="Center"
                          HorizontalOptions="Center"
                          Color="Blue"/>
    </Grid>
</ContentPage>